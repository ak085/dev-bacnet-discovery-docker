# Bridge Configuration File
# This file is managed by the BacPipes Web UI
# Manual edits may be overwritten

# Bridge is DISABLED by default
# Enable via Web UI: Settings → Cloud Integration → Enable Bridge
